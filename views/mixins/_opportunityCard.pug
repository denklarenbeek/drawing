mixin opportunityCard(opp = {})
    .opp-container
        p.name #{opp.name}
        p.amount #{opp.amount}
        p.scotsman #{opp.scotsman}
        p.weighted_amount #{opp.weighted_amount}
        - var date = h.moment(opp.timing).format("Q")
        p.month #{date === 'Invalid date' ? "Q" : `Q${date}`}
        button.btn
            i(class="fal fa-trash")
